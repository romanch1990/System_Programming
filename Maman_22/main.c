/*
 ============================================================================
 Name        : set.c
 Author      : Roman Chikunov
 Description : This program interact with the user and asks for commands to manipulate sets.
			There are 6 commands that the user can invoke and 5 of them to manipulate sets.
			1. read_set ; to read a set
			2. print_set ; to print a set
			3. union_set ; to unify two sets 
			4. intersect_set ; to intersect teo sets
			5. sub_set ; to subtract a set from another
			6. halt ; to close the program. 
 ============================================================================
 */

#include "set.h"



int main(void) {
	start();
	return 0;
}
